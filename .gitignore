# Local ComfyUI sample images — scratch/staging dump (~28 MB), full images with prompts/docs.
# The committed test corpus is built from these with `chandra scrub` (anonymized skeletons:
# no pixels, no prose, just graph structure) and lives in tests/fixtures/.
00_stuff/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# PDM
.pdm-python
.pdm-build/
# local dev interpreter pin; lockfile + requires-python cover reproducibility
.python-version

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
